from setuptools import setup

setup(
            name = 'graphtoarithmetic',
            version = '0.1',
            description='helper package for transforming a graph to arithmetic',
            author = 'Richard Fechner',
            packages=['graphtoarithmetic'],
            include_package_data = True,
            #package_data = { 'blendercharacters':['jkl.asd'] },
)
